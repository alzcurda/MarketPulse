version: '3.8'

services:
  marketpulse:
    build: .
    container_name: marketpulse-web
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - NO_BROWSER=1
